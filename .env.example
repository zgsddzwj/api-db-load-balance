# ============================================================================
# 数据库配置 - 主库（写操作）
# ============================================================================

# 方式一：使用完整URL（推荐）
# DB_PRIMARY_URL=mysql+pymysql://user:password@host:port/database

# 方式二：使用单独配置项
DB_PRIMARY_HOST=127.0.0.1
DB_PRIMARY_PORT=3306
DB_PRIMARY_USER=root
DB_PRIMARY_PASSWORD=your_password
DB_PRIMARY_DATABASE=app_db

# ============================================================================
# 数据库配置 - 从库（读操作）
# ============================================================================

# 方式一：使用完整URL（推荐）
# DB_REPLICA_URL=mysql+pymysql://user:password@host:port/database

# 方式二：使用单独配置项
DB_REPLICA_HOST=127.0.0.1
DB_REPLICA_PORT=3306
DB_REPLICA_USER=root
DB_REPLICA_PASSWORD=your_password
DB_REPLICA_DATABASE=app_db

# ============================================================================
# 使用说明
# ============================================================================
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 修改上述配置为实际数据库地址和账号
# 3. 如果设置了 DB_PRIMARY_URL 或 DB_REPLICA_URL，将优先使用URL方式
# 4. 如果未设置URL，将使用单独配置项（HOST/PORT/USER/PASSWORD/DATABASE）拼接
